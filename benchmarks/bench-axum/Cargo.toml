[package]
name = "bench-axum"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
axum = "0.8.9"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net"] }

[profile.release]
lto = true
codegen-units = 1
